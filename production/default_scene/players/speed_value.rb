module SpeedValue
  def mouse_clicked(event)
    # current_speed_value = self.text
    # scene.find('input_box_list').build do
    #   speed_input_box :players => 'text_box', :text => current_speed_value, :id => 'speed_input_box'
    # end
    # speed_value.text = ''
    # scene.build do
    #   speed_input_box :players => 'text_box', :text => current_value, :id => 'speed_input_box'
    # end
  end
end